package com.mashup.dorabangs.domain.clipboard.usecase

class SampleUseCase {
    fun baboTest(bool: Boolean) {
        if (bool) {
            println("너는 바보야?")
        } else {
            println("너는 가짜 바보야")
        }
    }
}
