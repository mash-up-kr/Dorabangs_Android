name: Auto Assignees

on:
  pull_request:
    types:
      - opened

jobs:
  add-auto-assignees:
    runs-on: ubuntu-latest

    steps:
      - name: add assignees
        run: |
          curl -X POST https://api.github.com/repos/mash-up-kr/Dorabangs_Android/issues/${{ github.event.number }}/assignees \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            -d '{"assignees": ${{ github.actor }} }'
