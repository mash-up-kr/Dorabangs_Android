name: Android CI

on:
  workflow_dispatch:
  pull_request:
    branches: [ "feature**", "hotfix**", "release**", "develop**", "master" ]

jobs:
  common_setup:
    uses: mash-up-kr/Dorabangs_Android/.github/workflows/common-setup.yaml@26e2283274779a9ab32d8ecaf764fad94aaac5c4

  build:
    needs: common_setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download common setup files
        uses: actions/download-artifact@v4
        with:
          name: common-setup-files

      - name: Build with Gradle
        run: ./gradlew assembleDebug
